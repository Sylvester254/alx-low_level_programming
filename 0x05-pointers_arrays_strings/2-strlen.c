#include "main.h"
#include <string.h>

/**
 * _strlen - function that returns length of a string.
 * @s: a string
 * Return: integer value.
 */
int _strl en(char *s)
{
int i;

i = strlen(s);
return (i);
}
